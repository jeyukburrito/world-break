Author: Codex

# 2026-04-05-codex

## T-032 Remove Share Images
- Removed the deprecated URL-based share pages, OG routes, components, and helper library.
- Removed the tournament share CTA and share payload code from `app/matches/page.tsx`.
- Updated both middleware public path lists so `/share` is no longer public while the retained `/api/og` PNG routes stay accessible.

## Files Touched
- `.ai/handoffs/T-032-result.md`
- `.ai/daily/2026-04-05-codex.md`
- `.ai/TASKS.md`
- `app/matches/page.tsx`
- `middleware.ts`
- `lib/supabase/middleware.ts`
- `app/api/og/match/route.ts` (deleted)
- `app/api/og/tournament/route.ts` (deleted)
- `app/share/match/page.tsx` (deleted)
- `app/share/tournament/page.tsx` (deleted)
- `components/share-button.tsx` (deleted)
- `components/match-share-og-card.tsx` (deleted)
- `components/tournament-share-og-card.tsx` (deleted)
- `lib/share/match-share.ts` (deleted)
- `lib/share/og-font.ts` (deleted)

## Validation
- `npm.cmd run lint`: PASS
- `npm.cmd run build`: FAIL because `DIRECT_URL` was not loaded into the shell environment
- `Get-Content .env.local | ForEach-Object { if ($_ -match '^(?!\\s*#)([^=]+)=(.*)$') { $name = $matches[1].Trim(); $value = $matches[2].Trim(); if (($value.StartsWith('"') -and $value.EndsWith('"')) -or ($value.StartsWith("'") -and $value.EndsWith("'"))) { $value = $value.Substring(1, $value.Length - 2) }; [System.Environment]::SetEnvironmentVariable($name, $value, 'Process') } }; npm.cmd run build` outside sandbox: PASS

## Risks / Follow-up
- Old `/share/*` links will stop working after deployment by design.
- `T-033` should branch from this cleaned state after `T-032` review/approval.

## T-033 Adaptive Shell UI
- Added a desktop-only `SideNav` with shared active-route logic, brand text, primary navigation, and a filled `+ 새 매치 입력` button.
- Updated `AppShell` to reserve left navigation space on desktop and expand the main content width to `md:max-w-3xl`.
- Hid `TopAppBar` and `BottomNav` at `md` and above while keeping the existing mobile shell unchanged below the breakpoint.

## Files Touched
- `.ai/handoffs/T-033-result.md`
- `.ai/daily/2026-04-05-codex.md`
- `.ai/TASKS.md`
- `components/side-nav.tsx`
- `components/app-shell.tsx`
- `components/top-app-bar.tsx`
- `components/bottom-nav.tsx`
- `lib/navigation.ts`

## Validation
- `npm.cmd run lint`: PASS
- `Get-Content .env.local | ForEach-Object { if ($_ -match '^(?!\\s*#)([^=]+)=(.*)$') { $name = $matches[1].Trim(); $value = $matches[2].Trim(); if (($value.StartsWith('"') -and $value.EndsWith('"')) -or ($value.StartsWith("'") -and $value.EndsWith("'"))) { $value = $value.Substring(1, $value.Length - 2) }; [System.Environment]::SetEnvironmentVariable($name, $value, 'Process') } }; npm.cmd run build`: PASS

## Risks / Follow-up
- Manual browser verification for the `md` breakpoint behavior is still pending.
