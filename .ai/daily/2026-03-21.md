# 2026-03-21

## Summary
- Replaced app-facing icon assets so the web app uses the new icon image.
- Optimized dashboard performance with cached aggregates, targeted dashboard invalidation, and top-matchup preview queries limited in SQL.
- Added dashboard-oriented Prisma indexes for `match_results`.
- Reduced authenticated request write load by moving full user profile sync to the OAuth callback and downgrading request-time sync to create-if-missing fallback.
- Updated `AGENTS.md` to require daily work logs under `.ai/daily/`.

## Files Touched
- `AGENTS.md`
- `app/auth/callback/route.ts`
- `app/dashboard/page.tsx`
- `app/matches/actions.ts`
- `app/matches/export/route.ts`
- `app/matches/tournaments/end/route.ts`
- `app/settings/decks/actions.ts`
- `app/settings/games/actions.ts`
- `components/dashboard-charts.tsx`
- `lib/auth.ts`
- `lib/dashboard.ts`
- `prisma/schema.prisma`
- `prisma/migrations/20260321031500_add_dashboard_indexes/migration.sql`

## Validation
- `npm.cmd run build`

## Risks / Follow-up
- `requireUser()` still performs a fallback existence check for legacy sessions; this is intentional for safety.
- Dashboard aggregate SQL is still split across multiple queries; data review recommended deferring a CTE/JSON aggregation rewrite until measurements show it is worthwhile.

## T-011 Session
- Implemented PWA `start_url` alignment, moved the dashboard period filter into the top app bar, and removed duplicate in-body page titles on dashboard, matches, and settings.

### T-011 Files Touched
- `.ai/handoffs/T-011-result.md`
- `app/dashboard/page.tsx`
- `app/matches/page.tsx`
- `app/settings/page.tsx`
- `public/manifest.json`

### T-011 Validation
- `npm.cmd run build`



## T-012 Session
- Added guest entry UI on the login page and guest banner integration in `AppShell`.

### T-012 Files Touched
- `app/login/actions.ts`
- `app/login/page.tsx`
- `components/app-shell.tsx`
- `components/guest-banner.tsx`
- `.ai/handoffs/T-012-result.md`

### T-012 Validation
- `npm.cmd run build`


## T-012 Backend Session
- Added guest-mode backend support with UUID-backed guest users, guest cookie bootstrapping in middleware, and auth fallback to guest sessions when Supabase is absent.
- Kept Supabase auth flow intact by routing authenticated users through the existing callback/profile sync path.
- Marked Supabase env vars as optional in the example env file so guest-mode startup is documented correctly.
- Added guest-aware profile deletion handling and opened an explicit `/login?guest=upgrade` path so guest users can actually reach Google login from the guest banner.
- Revised guest-mode copy to avoid implying unsupported automatic data migration from guest records to Google accounts.

### T-012 Backend Files Touched
- `.env.local.example`
- `.ai/handoffs/T-012-result.md`
- `.ai/reviews/T-012-review.md`
- `app/login/page.tsx`
- `app/settings/profile/actions.ts`
- `app/settings/profile/page.tsx`
- `components/guest-banner.tsx`
- `lib/auth.ts`
- `lib/guest.ts`
- `middleware.ts`
- `prisma/schema.prisma`
- `prisma/migrations/20260321042000_add_guest_mode/migration.sql`

### T-012 Backend Validation
- `npm.cmd run prisma:generate`
- `npm.cmd run build`

### T-012 Backend Risks / Follow-up
- Guest sign-in upgrade still starts a separate authenticated account; guest data migration remains out of scope.
- Guest records are intentionally stored as UUID users with a unique `guestToken`; if the cookie is manually tampered with, a new guest row can still be created.
