Author: Codex

# 2026-03-23-codex

## T-017 Match Share Card
- Applied the `T-017` spec to add a stateless match sharing flow based on query-string URLs only.
- Added a public `/share/match` page with OG / Twitter metadata and a 1200x630 `next/og` image route at `/api/og/match`.
- Added share buttons to match cards, using Web Share API first and clipboard-copy toast fallback second.
- Kept match actions, Prisma schema, and auth flow unchanged; only public-path allowances were added for `/share` and `/api/og`.
- Wrote the implementation summary to `.ai/handoffs/T-017-result.md`.

## Files Touched
- `app/api/og/match/route.ts`
- `app/share/match/page.tsx`
- `app/matches/page.tsx`
- `components/share-button.tsx`
- `components/match-share-og-card.tsx`
- `components/toast.tsx`
- `lib/share/match-share.ts`
- `lib/share/og-font.ts`
- `lib/toast.ts`
- `middleware.ts`
- `lib/supabase/middleware.ts`
- `.ai/handoffs/T-017-result.md`
- `.ai/daily/2026-03-23-codex.md`

## Validation
- `npm.cmd run lint`: PASS
- `npm.cmd run build`: FAIL (`DIRECT_URL` not exported into the shell for Prisma CLI)
- `Get-Content .env.local | ForEach-Object { ... }; npm.cmd run build`: FAIL due unrelated pre-existing type error in `./~/gstack/browse/src/browser-manager.ts` (`Cannot find module 'playwright'`)
- Unrestricted font probe:
  - `Noto Sans KR` subset fetch measured about `676ms` to `681ms` per tested weight
  - `Inter` subset fetch measured about `639ms` to `670ms` per tested weight

## Risks / Follow-up
- Full build verification remains blocked by the unrelated `~/gstack/**` Playwright type dependency issue.
- The code keeps the spec-required Noto spike threshold logic, but local network measurements suggest the runtime will often fall back to `Inter` unless deployment-side font fetch latency is lower.
