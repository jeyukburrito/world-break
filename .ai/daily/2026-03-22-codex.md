Author: Codex

# 2026-03-22-codex

## T-015 UI Microcopy & Information Architecture Cleanup
- Applied the `T-015` spec in the four scoped code files only.
- Converted relative match dates to absolute date formatting, removed redundant page-level text blocks, and differentiated the dashboard stat-card messaging.
- Wrote the implementation summary to `.ai/handoffs/T-015-result.md`.

## Files Touched
- `lib/format-date.ts`
- `app/matches/page.tsx`
- `app/matches/new/page.tsx`
- `components/dashboard-charts.tsx`
- `.ai/handoffs/T-015-result.md`
- `.ai/daily/2026-03-22-codex.md`

## Validation
- `npm.cmd run lint`: PASS
- `npm.cmd run build`: FAIL (`DIRECT_URL` not exported into the shell for Prisma CLI)
- `npm.cmd run build` with `.env.local` loaded into the process environment: FAIL (unrelated pre-existing type error in `./~/gstack/browse/src/browser-manager.ts`: `Cannot find module 'playwright'`)

## Risks / Follow-up
- Full build verification is still blocked by the unrelated `~/gstack/**` Playwright dependency issue outside the T-015 change set.
- `lib/format-date.ts` keeps a compatibility alias for `formatRelativeDate` so the four-file scope can be preserved while the app uses the new absolute-date behavior.

## T-016 UI Cleanup
- Applied the `T-016` spec in the five scoped files only.
- Removed the guest banner UI path from `AppShell`, deleted the banner component, added a dashboard link to the top app-bar brand, simplified the settings profile card, and updated the login guest CTA copy.
- Wrote the implementation summary to `.ai/handoffs/T-016-result.md`.

## Files Touched
- `app/login/page.tsx`
- `components/app-shell.tsx`
- `components/guest-banner.tsx` (deleted)
- `components/top-app-bar.tsx`
- `app/settings/page.tsx`
- `.ai/handoffs/T-016-result.md`
- `.ai/daily/2026-03-22-codex.md`

## Validation
- `rg -n "GuestBanner|guest-banner|isGuest\?: boolean|showGuestBanner|hasGuestCookie|게스트로 체험하기" app components lib`: PASS
- `npm.cmd run lint`: PASS
- `npm.cmd run build`: FAIL (`DIRECT_URL` not exported into the shell for Prisma CLI)
- `npm.cmd run build` with `.env.local` loaded into the process environment: FAIL (unrelated pre-existing type error in `./~/gstack/browse/src/browser-manager.ts`: `Cannot find module 'playwright'`)

## Risks / Follow-up
- Full build verification is still blocked by the unrelated `~/gstack/**` Playwright dependency issue outside the T-016 change set.
