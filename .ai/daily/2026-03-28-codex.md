Author: Codex

# 2026-03-28-codex

## T-022 OAuth Callback URL Fix
- Investigated `redirectTo`, `window.location.origin`, `VERCEL_URL`, `NEXT_PUBLIC_VERCEL_URL`, `/auth/callback`, and `signInWithOAuth` usage across the repo.
- Confirmed the login callback was built in `app/login/actions.ts` from request headers and could inherit a non-canonical Vercel host.
- Added a canonical auth callback origin helper in `lib/env.ts` so production OAuth always uses `https://world-break.vercel.app` while localhost keeps its local callback URL.
- Updated `app/login/actions.ts` to build `redirectTo` from the new helper.
- Updated `docs/SUPABASE_SETUP.md` and `docs/DEPLOYMENT.md` with the exact Redirect URL / Site URL values required for Supabase and Google OAuth setup.
- Updated `.ai/handoffs/T-022-spec.md` and wrote `.ai/handoffs/T-022-result.md`.

## Files Touched
- `lib/env.ts`
- `app/login/actions.ts`
- `docs/SUPABASE_SETUP.md`
- `docs/DEPLOYMENT.md`
- `.ai/handoffs/T-022-spec.md`
- `.ai/handoffs/T-022-result.md`
- `.ai/daily/2026-03-28-codex.md`

## Validation
- `npm.cmd run lint`: PASS
- `npm.cmd run build`: FAIL (`DIRECT_URL` missing during Prisma config validation)
- `rg -n "redirectTo|window\\.location\\.origin|VERCEL_URL|NEXT_PUBLIC_VERCEL_URL|/auth/callback|signInWithOAuth" app lib components docs -S`: PASS

## Risks / Follow-up
- Supabase `Authentication > URL Configuration` must include both `http://localhost:3000/auth/callback` and `https://world-break.vercel.app/auth/callback`.
- Google OAuth production verification should be run after the Supabase allow list is confirmed.

## T-023 Server Action Integration Tests
- Added `vitest` and wired `npm run test` to `vitest run`.
- Added `vitest.config.ts`, `__tests__/setup.ts`, and `__tests__/server-actions.integration.test.ts`.
- Implemented DB-backed integration coverage for `createMatchResult`, `updateMatchResult`, `deleteMatchResult`, and `deleteAccount`.
- Validation:
  - `npm.cmd run lint`: PASS
  - `npm.cmd run test`: FAIL after suite startup because Prisma rejects the datasource URL and expects `prisma://` / `prisma+postgres://`
  - `npm.cmd run build`: FAIL (`DIRECT_URL` missing during Prisma config validation)
- Open risks / follow-up:
  - Local Prisma runtime/test DB configuration needs to be fixed before this suite can be used as a real regression gate.

## T-024 Guest Orphan Cleanup
- Added `scripts/cleanup-guests.mjs` for expired guest cleanup with dry-run by default and `--apply` for actual deletion.
- Added `cleanup:guests` script to `package.json`.
- Validation:
  - `node --check scripts/cleanup-guests.mjs`: PASS
  - `npm.cmd run lint`: PASS
  - runtime execution remains blocked by the same local Prisma datasource issue
- Open risks / follow-up:
  - Cleanup is manual for now; scheduling can be added later if needed.

## T-025 Remove Unused Tag Tables
- Removed `Tag` / `MatchResultTag` from Prisma schema and deleted the unused tag validation module.
- Removed tag-related query/action/UI code from match flows and added `prisma/migrations/20260328183000_remove_tag_tables/migration.sql`.
- Verified `docs/ARCHITECTURE.md` already matched the post-tag model graph, so no doc edit was needed there.
- Validation:
  - `npm.cmd run lint`: PASS
  - `rg -n "\\bTag\\b|MatchResultTag|settings/tags|tag-selector" app lib components prisma docs -S`: PASS for active code, with expected hits only in migrations and analytics labels
  - `npm.cmd run build`: FAIL (`DIRECT_URL` missing during Prisma config validation)
- Open risks / follow-up:
  - Applying the migration still depends on a working local Prisma database configuration.
## T-028 BO3 Play Sequence Backend/Data Slice
- Added nullable `bo3PlaySequence` to `prisma/schema.prisma` and created `prisma/migrations/20260328193000_add_bo3_play_sequence/migration.sql`.
- Extended `lib/validation/match.ts` to accept and normalize BO3 play-sequence input.
- Updated `app/matches/actions.ts` to persist the sequence on create/update, preserving the previous value when an edit does not submit a new one.
- Extended `lib/matches.ts`, `lib/group-matches.ts`, and `app/matches/[id]/edit/page.tsx` so the data layer carries the new field for later UI/display work.
- Validation:
  - `npm.cmd run lint`: PASS
  - `npm.cmd run build`: FAIL (`DIRECT_URL` missing during Prisma config validation)
- Open risks / follow-up:
  - Frontend/display wiring still needs the separate UI pass.
  - Build verification remains blocked by the local Prisma environment issue.

## T-027 Top Bar Icon Removal
- Removed the logo icon from `components/top-app-bar.tsx` and left the linked `World Break` text only.
- Validation:
  - `npm.cmd run lint`: PASS
  - `npm.cmd run build`: FAIL (`DIRECT_URL` missing during Prisma config validation)

## T-028 BO3 Play Sequence Tracking Final Pass
- Added nullable `bo3PlaySequence` to `prisma/schema.prisma` and created `prisma/migrations/20260328193000_add_bo3_play_sequence/migration.sql`.
- Extended `lib/validation/match.ts`, `app/matches/actions.ts`, `lib/matches.ts`, and `lib/group-matches.ts` so the new field is validated, persisted, and loaded.
- Added BO3 sequence controls in `components/match-result-input.tsx` and synchronized BO3 format state with `components/match-detail-controls.tsx`.
- Wired the saved sequence into `app/matches/[id]/edit/page.tsx` and displayed formatted sequences like `선후선` in `app/matches/page.tsx`.
- Validation:
  - `npm.cmd run lint`: PASS
  - `npm.cmd run build`: FAIL (`DIRECT_URL` missing during Prisma config validation)

## T-029 Play-Order Chooser Wording
- Renamed the form label to `선택 주체` and changed the options to `내가 선택` / `상대가 선택`.
- Updated `app/matches/page.tsx` so match cards always display who chose play order explicitly.
- Validation:
  - `npm.cmd run lint`: PASS
  - `npm.cmd run build`: FAIL (`DIRECT_URL` missing during Prisma config validation)

## T-030 Match Card Result Emphasis
- Increased the win/loss pill size and contrast in `app/matches/page.tsx`.
- Added result-based tint and ring styling to both single-match cards and tournament round cards.
- Validation:
  - `npm.cmd run lint`: PASS
  - `npm.cmd run build`: FAIL (`DIRECT_URL` missing during Prisma config validation)
