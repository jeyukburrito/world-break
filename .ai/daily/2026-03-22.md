# 2026-03-22

## Summary
- Completed `T-013` backend & DB audit across Prisma schema, server actions, API routes, auth/guest flows, and dashboard raw SQL.
- Wrote the audit report to `.ai/handoffs/T-013-result.md` with `7 FAIL`, `9 WARN`, `14 PASS`.
- Confirmed the current tree passes lint, and recorded that Prisma schema validation could not complete because the environment blocked the required engine download.

## Files Touched
- `.ai/daily/2026-03-22.md`
- `.ai/handoffs/T-013-result.md`

## Validation
- `npm.cmd run lint`: PASS
- `npx.cmd prisma validate`: FAIL (`schema-engine` download blocked by network restriction)

## Risks / Follow-up
- Legacy `cs` category handling is inconsistent: the server still accepts it, but current "대회" filters only surface `shop`.
- Some mutation paths still lack UUID validation or `count === 0` handling and can return false-positive success or unhandled errors.
- CSV export still lacks formula-injection defense and currently materializes the full export in memory.

## AGENTS Sync Session
- Updated `AGENTS.md` to align it with the applicable project-level rules from `CLAUDE.md`.
- Added the architecture reference, working principles, verification expectations, and an explicit ban on creating a parallel `tasks/` workflow outside `.ai/`.

### AGENTS Sync Files Touched
- `AGENTS.md`
- `.ai/daily/2026-03-22.md`

### AGENTS Sync Validation
- Re-read `AGENTS.md` after editing to confirm the new sections were saved as intended.

### AGENTS Sync Risks / Follow-up
- `CLAUDE.md` still contains environment-specific instructions such as `gstack` browsing and legacy `tasks/` references; these were intentionally not copied into `AGENTS.md`.

## Build Fix Session
- Fixed `prisma/migrations/20260322190000_cleanup_eventcategory_enum/migration.sql` so the cleanup migration no longer retargets `eventCategory` through `text` while the old `chk_tournament_phase_category` constraint is still active.
- The migration now follows a safer order: drop default/constraint, normalize `cs` data, recreate or reconnect the enum, then restore the default and the updated check constraint.
- Kept the fix scoped to the failing migration so the existing deployment flow remains unchanged.

### Build Fix Files Touched
- `prisma/migrations/20260322190000_cleanup_eventcategory_enum/migration.sql`
- `.ai/daily/2026-03-22.md`

### Build Fix Validation
- `npm.cmd run lint`: PASS
- Local `npm.cmd run build`: not run in this workspace because `.env.local` is absent and the build script requires a live Prisma database connection.

### Build Fix Risks / Follow-up
- The production/preview build should be re-run against the real database to confirm the repaired migration applies cleanly.
- `package.json` still resolves `20260322180000_migrate_cs_to_shop` as applied during build; this remains intentional and was not changed in this fix.

## Build Verification Session
- Removed the one-off `prisma migrate resolve --applied 20260322180000_migrate_cs_to_shop` step from `package.json` because it makes repeated builds fail once that migration is already recorded as applied.
- Marked the failed `20260322190000_cleanup_eventcategory_enum` migration as rolled back in the target database, then re-ran the repaired migration through the normal `prisma migrate deploy` path.
- Verified the full production build end to end against the configured database.

### Build Verification Files Touched
- `package.json`
- `prisma/migrations/20260322190000_cleanup_eventcategory_enum/migration.sql`
- `.ai/daily/2026-03-22.md`

### Build Verification Validation
- `npx.cmd prisma migrate resolve --rolled-back 20260322190000_cleanup_eventcategory_enum`: PASS
- `npm.cmd run build`: PASS

### Build Verification Risks / Follow-up
- The migration history in the target database now reflects the repaired `20260322190000_cleanup_eventcategory_enum` as successfully applied.
- Local Prisma CLI still requires environment variables to be exported into the shell because it does not auto-load `.env.local`.

## UTF-8 Analytics Patch Session
- Patched the GA flash-event parameter encoding flow to stop using raw `btoa/atob` on JSON strings, which was throwing `InvalidCharacterError` when game or deck names contained Korean or Japanese text.
- Added a shared UTF-8-safe base64url helper and switched all current flash-event producers and consumers to that helper.
- Hardened analytics param decoding so malformed legacy `ep` values are ignored instead of breaking the `/matches/new` page render.

### UTF-8 Analytics Patch Files Touched
- `lib/base64url.ts`
- `components/analytics.tsx`
- `app/matches/actions.ts`
- `app/auth/callback/route.ts`
- `app/matches/tournaments/end/route.ts`
- `app/settings/decks/actions.ts`
- `app/settings/games/actions.ts`
- `.ai/daily/2026-03-22.md`

### UTF-8 Analytics Patch Validation
- `npm.cmd run lint`: PASS
- `npm.cmd run build` (with `.env.local` loaded into process env): PASS

### UTF-8 Analytics Patch Risks / Follow-up
- Existing URLs that still carry the old `ep` format may not decode into analytics params until they are regenerated, but the page no longer crashes because malformed values are now skipped.
