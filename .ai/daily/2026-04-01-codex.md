Author: Codex

# 2026-04-01-codex

## OAuth Callback Canonical Host Hotfix
- Added a callback-host normalization guard in `app/auth/callback/route.ts`.
- When `/auth/callback` is hit on a non-local non-canonical host, the route now issues a `307` redirect to `https://world-break.vercel.app/auth/callback` with the original query string preserved before exchanging the Supabase auth code.

## Middleware Canonical Host Hotfix
- Added a global host normalization guard in `middleware.ts`.
- When any app request arrives on a non-local non-canonical host, middleware now issues a `307` redirect to the same path/query on `https://world-break.vercel.app` before auth or guest routing runs.

## Match Detail Copy / Mobile Size Fix
- Updated `components/match-detail-controls.tsx` so the secondary segmented control label reads `선후 결정`.
- Shortened the option labels from `내가 선택` / `상대가 선택` to `자신` / `상대`.
- Added `whitespace-nowrap` to segmented buttons so the mobile control height stays aligned with the `선후공` control.

## Files Touched
- `.ai/daily/2026-04-01-codex.md`
- `app/auth/callback/route.ts`
- `middleware.ts`
- `components/match-detail-controls.tsx`

## Validation
- `npm.cmd run lint`: PASS

## Risks / Follow-up
- These hotfixes harden the app against preview-host access, but Supabase `Site URL` / `Redirect URLs` should still be cleaned up so the provider flow starts on the canonical domain consistently.
- Full mobile verification still needs a real browser test after deployment, especially if old PWA/site data exists on the device.
