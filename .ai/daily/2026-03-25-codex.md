Author: Codex

# 2026-03-25-codex

## T-021 OG Font Bundling
- Applied the `T-021` spec to remove runtime Google Fonts fetches from the match/tournament OG image routes.
- Generated bundled `NotoSansKR-Regular.woff2` and `NotoSansKR-Bold.woff2` under `public/fonts` from the local `C:\Windows\Fonts\NotoSansKR-VF.ttf` source.
- Replaced `lib/share/og-font.ts` with local disk reads plus a static in-process promise cache.
- Removed the now-dead OG font text helpers from `lib/share/match-share.ts`.
- Updated both OG routes to call `loadMatchOgFonts()` without payload-specific text arguments.
- Wrote the implementation summary to `.ai/handoffs/T-021-result.md`.
- Because Gemini was unavailable, ran a separate Codex-invoked sub-agent review and recorded it in `.ai/daily/T-021-review-subagent.md`.

## Files Touched
- `public/fonts/NotoSansKR-Regular.woff2`
- `public/fonts/NotoSansKR-Bold.woff2`
- `lib/share/og-font.ts`
- `lib/share/match-share.ts`
- `app/api/og/match/route.ts`
- `app/api/og/tournament/route.ts`
- `.ai/TASKS.md`
- `.ai/handoffs/T-021-result.md`
- `.ai/daily/2026-03-25-codex.md`
- `.ai/daily/T-021-review-subagent.md`

## Validation
- `npm.cmd run lint`: PASS
- `npm.cmd run build`: FAIL (`DIRECT_URL` not exported for Prisma config validation)
- `npx.cmd next build`: FAIL due unrelated pre-existing type error in `./~/gstack/browse/src/browser-manager.ts` (`Cannot find module 'playwright'`)
- `rg -n "buildMatchOgFontText|buildTournamentOgFontText|loadMatchOgFonts\\(" app lib -S`: PASS

## Risks / Follow-up
- Full build verification remains blocked by the unrelated `DIRECT_URL` env requirement in the package build step and the unrelated `~/gstack/**` Playwright type dependency issue.
- The runtime network dependency is removed, but the review sub-agent flagged that the current bundled fonts are still larger than the spec intended, so T-021 should stay in progress until a smaller asset strategy is chosen.
